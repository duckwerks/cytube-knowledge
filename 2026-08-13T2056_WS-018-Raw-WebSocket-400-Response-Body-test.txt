~ $ echo 'WS-018
ders,
        timeout=10,
    )

    print("HTTP:", r.status_code)
    print("Content-Type:", r.headers.get("content-type"))
    print("Response:", repr(r.text[:1000]))

except Exception as e:
    print("HANDSHAKE ERROR:", repr(e))
    raise SystemExit(1)

if r.status_code != 200 or not r.text.startswith("0"):
    print("HANDSHAKE FAILED")
    raise SystemExit(1)

try:
    handshake = json.loads(r.text[1:])
except Exception as e:
    print(> Date: 2026-08-13
__)
    print("EXCEPTION:", repr(e))
    print()

    # websocket-client exposes the HTTP response headers when
    # the server rejects the WebSocket upgr> Purpose: Capture the complete HTTP 400 response returned by cytu.be when an Engine.IO WebSocket upgrade is attempted. WS-017 established that polling works, fresh SIDs remain valid, and multiple WebSocket client implementations all receive HTTP 400. This test identifies whether the rejection is generated by Cloudflare, the Cytube server, Engine.IO, or another proxy layer.
> Target: https://cytu.be/r/EMAIL
> Evidence type: EXPERIMENTALLY-CONFIRMED
>
> === WS-018 RAW WEBSOCKET 400 RESPONSE ===' &&
> python -m pip install --upgrade websocket-client &&
> python - <<'PY'
> import requests
> import websocket
e> import ssl
> import json
> import socket
> import time
>
> BASE = "https://cytu.be"
> ROOM = "https://cytu.be/r/EMAIL"
> UA = "Mozilla/5.0 (Android 10; Mobile; rv:153.0) Gecko/153.0 Firefox/153.0"
>
v> print("=== TEST PARAMETERS ===")
 > print("HTTP :", BASE)
> print("ROOM :", ROOM)
> print("UA   :", UA)
> print()
>
> # ------------------------------------------------------------
> # STEP 1
> # Obtain a fresh Engine.IO polling SID.
g&> # ------------------------------------------------------------
>

> print("=== STEP 1: FRESH ENGINE.IO HANDSHAKE ===")
>
> session = requests.Session()
>
> headers = {
>     "User-Agent": UA,
>     "Origin": BASE,
>     "Referer": ROOM,
>     "Accept": "*/*",
>     "Cache-Control": "no-cache",
>     "Pragma": "no-cache",
> }
in>
> try:
>     r = session.get(
>         BASE + "/socket.io/?EIO=4&transport=polling",
>         headers=headers,
>         timeout=10,
>     )
>
>     print("HTTP:", r.status_code)
>     print("Content-Type:", r.headers.get("content-type"))
>     print("Response:", repr(r.text[:1000]))
>
> except Exception as e:
>     print("HANDSHAKE ERROR:", repr(e))
>     raise SystemExit(1)
ody")
print("4. HTTPS tr>
> if r.status_code != 200 or not r.text.startswith("0"):
>     print("HANDSHAKE FAILED")
>     raise SystemExit(1)
>
> try:
>     handshake = json.loads(r.text[1:])
> except Exception as e:
>     print("JSON ERROR:", repr(e))
>     raise SystemExit(1)
>
sult")
print()
print("KEY QUESTION:")
print("Who generated the HTTP 400 response?")
print()
print("Possible classificat> sid = handshake["sid"]
ion>
> print("SID:", sid)
> print("Upgrades:", handshake.get("upgrades"))
> print("Ping interval:", handshake.get("pingInterval"))
> print("Ping timeout:", handshake.get("pingTimeout"))
> print()
>
> # ------------------------------------------------------------
> # STEP 2
> # Attempt the exact Engine.IO WebSocket upgrade.
> # ------------------------------------------------------------
>
> ws_url = (
>     "wss://cytu.be/socket.io/"
>     "?EIO=4&transport=websocket&sid="
>     + sid
> )
>
> print("=== STEP 2: WEBSOCKET UPGRADE ===")
> print("URL:", ws_url)
> print()
>
> try:
>     ws = websocket.create_connection(
>         ws_url,
>         timeout=10,
>         origin=BASE,
>         host="cytu.be",
>         header=[
>             "User-Agent: " + UA,
>             "Accept: */*",
>             "Cache-Control: no-cache",
>             "Pragma: no-cache",
>             "Referer: " + ROOM,
>         ],
>         sslopt={
>             "cert_reqs": ssl.CERT_REQUIRED,
>         },
>     )
>
>     print("RESULT: WEBSOCKET CONNECTED")
>     print()
>
>     try:
>         print("FIRST FRAME:", repr(ws.recv()))
>     except Exception as e:
>         print("FRAME ERROR:", repr(e))
>
>     ws.close()
>
> except Exception as e:
>
>     print("RESULT: WEBSOCKET FAILED")
>     print("EXCEPTION TYPE:", type(e).__name__)
>     print("EXCEPTION:", repr(e))
>     print()
>
>     # websocket-client exposes the HTTP response headers when
>     # the server rejects the WebSocket upgrade.
>     print("=== RAW REJECTION DETAILS ===")
>
>     if hasattr(e, "status_code"):
>         print("STATUS CODE:", repr(e.status_code))
>
>     if hasattr(e, "resp_headers"):
>         print()
>         print("--- RESPONSE HEADERS ---")
>         for k, v in e.resp_headers:
>             print(k + ":", v)
>
>     if hasattr(e, "resp_body"):
>         print()
>         print("--- RESPONSE BODY ---")
>         print(repr(e.resp_body))
>
>     print()
>
> # ------------------------------------------------------------
> # STEP 3
> # Make a normal HTTPS request to the same endpoint.
> #
> # This is NOT a WebSocket request. It is useful for determining
> # whether the HTTP endpoint itself exposes an Engine.IO error
> # body when given transport=websocket.
> # ------------------------------------------------------------
>
> print("=== STEP 3: HTTPS REQUEST TO WEBSOCKET TRANSPORT ===")
>
> http_ws_url = (
>     BASE
>     + "/socket.io/?EIO=4&transport=websocket&sid="
>     + sid
> )
>
> try:
>     r2 = session.get(
>         http_ws_url,
>         headers=headers,
>         timeout=10,
>     )
>
>     print("HTTP:", r2.status_code)
>     print("Content-Type:", r2.headers.get("content-type"))
>     print("Content-Length:", r2.headers.get("content-length"))
>     print()
>
>     print("--- RESPONSE HEADERS ---")
>     for k, v in r2.headers.items():
>         print(k + ":", v)
>
>     print()
>     print("--- RESPONSE BODY ---")
>     print(repr(r2.text[:5000]))
>
> except Exception as e:
>     print("HTTPS TEST ERROR:", repr(e))
>
> print()
>
> # ------------------------------------------------------------
> # STEP 4
> # Validate that the polling SID remains alive after the
> # rejected WebSocket attempt.
> # ------------------------------------------------------------
>
> print("=== STEP 4: SID CONTINUITY CHECK ===")
>
> poll_url = (
>     BASE
>     + "/socket.io/?EIO=4&transport=polling&sid="
>     + sid
> )
>
> try:
>     r3 = session.get(
>         poll_url,
>         headers=headers,
>         timeout=5,
>     )
>
>     print("HTTP:", r3.status_code)
>     print("Content-Type:", r3.headers.get("content-type"))
>     print("Response:", repr(r3.text[:1000]))
>
> except requests.exceptions.ReadTimeout:
>     print("RESULT: POLLING REMAINED OPEN / READ TIMEOUT")
>     print("SID appears to remain active.")
>
> except Exception as e:
>     print("POLLING ERROR:", repr(e))
>
> print()
> print("=== WS-018 CLASSIFICATION ===")
> print()
> print("Record:")
> print("1. WebSocket HTTP status")
> print("2. WebSocket rejection headers")
> print("3. WebSocket rejection body")
> print("4. HTTPS transport=websocket response")
> print("5. SID continuity result")
> print()
> print("KEY QUESTION:")
> print("Who generated the HTTP 400 response?")
> print()
> print("Possible classifications:")
> print("A. Cloudflare/proxy-generated rejection")
> print("B. Cytube application-generated rejection")
> print("C. Engine.IO-generated rejection")
> print("D. WebSocket transport/proxy mismatch")
> print("E. Other")
> print()
> print("=== WS-018 COMPLETE ===")
> PY
WS-018
Date: 2026-08-13
Purpose: Capture the complete HTTP 400 response returned by cytu.be when an Engine.IO WebSocket upgrade is attempted. WS-017 established that polling works, fresh SIDs remain valid, and multiple WebSocket client implementations all receive HTTP 400. This test identifies whether the rejection is generated by Cloudflare, the Cytube server, Engine.IO, or another proxy layer.
Target: https://cytu.be/r/EMAIL
Evidence type: EXPERIMENTALLY-CONFIRMED

=== WS-018 RAW WEBSOCKET 400 RESPONSE ===
Requirement already satisfied: websocket-client in /data/data/com.termux/files/usr/lib/python3.13/site-packages (1.9.0)
=== TEST PARAMETERS ===
HTTP : https://cytu.be
ROOM : https://cytu.be/r/EMAIL
UA   : Mozilla/5.0 (Android 10; Mobile; rv:153.0) Gecko/153.0 Firefox/153.0

=== STEP 1: FRESH ENGINE.IO HANDSHAKE ===
HTTP: 200
Content-Type: text/plain; charset=UTF-8
Response: '0{"sid":"bWlOXmoLeYDfWkl4BJm7","upgrades":["websocket"],"pingInterval":25000,"pingTimeout":120000,"maxPayload":1048576}'
SID: bWlOXmoLeYDfWkl4BJm7
Upgrades: ['websocket']
Ping interval: 25000
Ping timeout: 120000

=== STEP 2: WEBSOCKET UPGRADE ===
URL: wss://cytu.be/socket.io/?EIO=4&transport=websocket&sid=bWlOXmoLeYDfWkl4BJm7

RESULT: WEBSOCKET FAILED
EXCEPTION TYPE: WebSocketBadStatusException
EXCEPTION: WebSocketBadStatusException('Handshake status 400 Bad Request -+-+- {\'date\': \'Fri, 14 Aug 2026 03:56:29 GMT\', \'content-type\': \'application/json\', \'transfer-encoding\': \'chunked\', \'connection\': \'keep-alive\', \'server\': \'cloudflare\', \'nel\': \'{"report_to":"cf-nel","success_fraction":0.0,"max_age":604800}\', \'access-control-allow-origin\': \'https://cytu.be\', \'vary\': \'Origin\', \'access-control-allow-credentials\': \'true\', \'cf-cache-status\': \'DYNAMIC\', \'report-to\': \'{"group":"cf-nel","max_age":604800,"endpoints":[{"url":"https://a.nel.cloudflare.com/report/v4?s=HCw3TbniMRVyuCVkLWvRMS%2FOB25kaDHT5ElIHY635vCagNwCf8uI8ACGGu9w4QsHDfvU7x%2FzJTIOaW3Q20pc00eUAmP4jM4WFprrGD2HTR%2B4vXHWfk0SmmaYvHcSMCS3q5A%2Fg3f5"}]}\', \'cf-ray\': \'a2ad07accd404809-SJC\', \'alt-svc\': \'h3=":443"; ma=86400\'} -+-+- None')

=== RAW REJECTION DETAILS ===
STATUS CODE: 400

--- RESPONSE HEADERS ---
Traceback (most recent call last):
  File "<stdin>", line 85, in <module>
  File "/data/data/com.termux/files/usr/lib/python3.13/site-packages/websocket/_core.py", line 664, in create_connection
    websock.connect(url, **options)
    ~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^
  File "/data/data/com.termux/files/usr/lib/python3.13/site-packages/websocket/_core.py", line 268, in connect
    self.handshake_response = handshake(self.sock, url, *addrs, **options)
                              ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/data/data/com.termux/files/usr/lib/python3.13/site-packages/websocket/_handshake.py", line 66, in handshake
    status, resp = _get_resp_headers(sock)
                   ~~~~~~~~~~~~~~~~~^^^^^^
  File "/data/data/com.termux/files/usr/lib/python3.13/site-packages/websocket/_handshake.py", line 158, in _get_resp_headers
    raise WebSocketBadStatusException(
    ...<5 lines>...
    )
websocket._exceptions.WebSocketBadStatusException: Handshake status 400 Bad Request -+-+- {'date': 'Fri, 14 Aug 2026 03:56:29 GMT', 'content-type': 'application/json', 'transfer-encoding': 'chunked', 'connection': 'keep-alive', 'server': 'cloudflare', 'nel': '{"report_to":"cf-nel","success_fraction":0.0,"max_age":604800}', 'access-control-allow-origin': 'https://cytu.be', 'vary': 'Origin', 'access-control-allow-credentials': 'true', 'cf-cache-status': 'DYNAMIC', 'report-to': '{"group":"cf-nel","max_age":604800,"endpoints":[{"url":"https://a.nel.cloudflare.com/report/v4?s=HCw3TbniMRVyuCVkLWvRMS%2FOB25kaDHT5ElIHY635vCagNwCf8uI8ACGGu9w4QsHDfvU7x%2FzJTIOaW3Q20pc00eUAmP4jM4WFprrGD2HTR%2B4vXHWfk0SmmaYvHcSMCS3q5A%2Fg3f5"}]}', 'cf-ray': 'a2ad07accd404809-SJC', 'alt-svc': 'h3=":443"; ma=86400'} -+-+- None

During handling of the above exception, another exception occurred:

Traceback (most recent call last):
  File "<stdin>", line 129, in <module>
ValueError: too many values to unpack (expected 2)
~ $
